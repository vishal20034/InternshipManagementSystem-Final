# Current System Map

## Overview
TEN Internship Portal — manages student registration, task submission, attendance, certificates, and coordinator/HR workflows.

## Modules
- Student Portal (`/public/student-*`)
- HR Portal (`/public/hr-*`)
- Coordinator Portal (`/public/coordinator-*`)
- API Server (`server.js`, `routes/`, `routes/v2/`)
- Models (`models/`)
- Services (`services/`)
- Seeds (`seeds/`)
- Scripts (`scripts/`)
- Uploads (`uploads/`)

